let price = 100;
price = 150; /* значение переменной разрешается менять в случае, 
если сама переменная не объявляется повторно с присваиванием этого нового значения */
console.log(price); // консоль выведет последнее значение, без повторно объявленной переменной

